# coding: utf-8
"""Waylay Resources model tests.

This code was generated from the OpenAPI documentation of 'Waylay Resources'

Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json

from jsf import JSF
from pydantic import TypeAdapter

from ..openapi import MODEL_DEFINITIONS, with_example_provider

try:
    from waylay.services.resources.models.hal_resource_listing import HALResourceListing

    HALResourceListingAdapter = TypeAdapter(HALResourceListing)
    MODELS_AVAILABLE = True
except ImportError as exc:
    MODELS_AVAILABLE = False

hal_resource_listing_model_schema = json.loads(
    r"""{
  "required" : [ "_embedded", "_links", "count", "limit", "skip", "total" ],
  "type" : "object",
  "description" : "Listing of _Resource_ entities in HAL format",
  "allOf" : [ {
    "$ref" : "#/components/schemas/PagingResult"
  }, {
    "$ref" : "#/components/schemas/PagingCount"
  }, {
    "$ref" : "#/components/schemas/HALPageLinks"
  }, {
    "properties" : {
      "_embedded" : {
        "$ref" : "#/components/schemas/HALResourceListing_allOf__embedded"
      }
    }
  } ]
}
""",
    object_hook=with_example_provider,
)
hal_resource_listing_model_schema.update({"definitions": MODEL_DEFINITIONS})

hal_resource_listing_faker = JSF(
    hal_resource_listing_model_schema, allow_none_optionals=1
)


class HALResourceListingStub:
    """HALResourceListing unit test stubs."""

    @classmethod
    def create_json(cls):
        """Create a dict stub instance."""
        return hal_resource_listing_faker.generate(use_defaults=True, use_examples=True)

    @classmethod
    def create_instance(cls) -> "HALResourceListing":
        """Create HALResourceListing stub instance."""
        if not MODELS_AVAILABLE:
            raise ImportError("Models must be installed to create class stubs")
        json = cls.create_json()
        if not json:
            # use backup example based on the pydantic model schema
            backup_faker = JSF(
                HALResourceListingAdapter.json_schema(), allow_none_optionals=1
            )
            json = backup_faker.generate(use_defaults=True, use_examples=True)
        return HALResourceListingAdapter.validate_python(
            json, context={"skip_validation": True}
        )
